<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.daledawson.jetpackstudy">

    <!-- Declare features -->
    <uses-feature android:name="android.hardware.camera" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Declare permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <!--                <category android:name="android.intent.category.BROWSABLE" />-->

                <!--                <data android:scheme="http" />-->
                <!--                <data android:scheme="https" />-->
                <!--                <data android:host="www.chs.com" />-->
                <!--                <data android:pathPrefix="/" />-->
            </intent-filter>
            <!--            <nav-graph android:value="@navigation/nav_graph" />-->
        </activity>
        <activity android:name=".camerax.CameraXActivity" />
        <activity android:name=".room.RoomTestActivity" />
        <activity android:name=".Navigation.BottomNavActivity" /> <!--底部菜单-->
        <activity android:name=".Navigation.NavActivity" />
        <activity android:name=".databinding.DataBindingTest" />
        <activity android:name=".lifecycle.LifeCycleActivity" />
        <activity android:name=".DataStore.DataStoreActivity" />
        <activity android:name="Activity_Results_API.FirstActivity" />
        <activity android:name="Activity_Results_API.SecondActivity" />
        <activity android:name=".livedata.NameActivity" />
    </application>

</manifest>